<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.AndroidAssignments" parent="Base.Theme.AndroidAssignments">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/darker_gray</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
        <item name="android:actionBarItemBackground">@android:color/darker_gray</item>

    </style>
</resources>